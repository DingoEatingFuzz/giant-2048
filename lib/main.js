var chroma = require('chroma-js')
    , Grid = require('./grid')
;

/* this is where the game comes together.

make a game
make a renderer
attach the renderer to the game
attach the renderer to the canvas

*/
